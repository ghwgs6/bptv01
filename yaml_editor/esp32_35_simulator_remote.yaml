# -------------------------------------------------------------
# ESP32 3.5" TOUCHSCREEN SENSOR SIMULATOR & REMOTE TESTER
# Target Chip: ESP32-D0WD (3.5" ST7796 480x320 + XPT2046 Touch)
# Controls: Tap zone to toggle, Header Mode: [TAP: NORMAL / BYPASS], [ALL ON], [ALL OFF], [CLR BYP], [CYCLE]
# -------------------------------------------------------------
substitutions:
  device_name: esp32-35-simulator-remote
  friendly_name: "ESP32 3.5\" Sensor Simulator Remote"

esphome:
  name: ${device_name}
  friendly_name: ${friendly_name}
  includes:
    - esp_now_sender.h

esp32:
  board: esp32dev
  framework:
    type: esp-idf

# Wi-Fi configuration (multi-network with fallback AP)
wifi:
  networks:
    - ssid: "H824"
      password: "gw202001"
      priority: 10.0
    - ssid: "Pulse_Staff"
      password: "Pulse@1234"
      priority: 5.0
  fast_connect: true
  on_connect:
    then:
      - lambda: |-
          if (global_esp_now_sender != nullptr) {
            global_esp_now_sender->init();
          }
  ap:
    ssid: "ESP32-Remote-Fallback"
    password: "fallbackpassword"

api:
ota:
  - platform: esphome

logger:
  baud_rate: 115200

# -------------------------------------------------------------
# HARDWARE BACKLIGHT (LEDC PWM on GPIO27 @ 20kHz)
# -------------------------------------------------------------
output:
  - platform: ledc
    pin: GPIO27
    id: gpio_backlight_pwm
    frequency: 20kHz

light:
  - platform: monochromatic
    output: gpio_backlight_pwm
    name: "Display Backlight"
    id: disp_backlight
    restore_mode: ALWAYS_ON

# -------------------------------------------------------------
# SPI BUS (Shared between ST7796 Display and XPT2046 Touch)
# -------------------------------------------------------------
spi:
  - id: tft_spi
    clk_pin: GPIO14
    mosi_pin: GPIO13
    miso_pin: GPIO12

# -------------------------------------------------------------
# 18 SIMULATED DIGITAL INPUT & BYPASS GLOBALS
# -------------------------------------------------------------
globals:
  # 18 DI States (ON/OFF)
  - id: sim_di_01
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_02
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_03
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_04
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_05
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_06
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_07
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_08
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_09
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_10
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_11
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_12
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_13
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_14
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_15
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_16
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_17
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_di_18
    type: bool
    restore_value: no
    initial_value: 'false'

  # 18 DI Bypass States
  - id: sim_byp_01
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_02
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_03
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_04
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_05
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_06
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_07
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_08
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_09
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_10
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_11
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_12
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_13
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_14
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_15
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_16
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_17
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: sim_byp_18
    type: bool
    restore_value: no
    initial_value: 'false'

  # Mode & Touch Timers
  - id: bypass_mode_active
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: cycle_active
    type: bool
    restore_value: no
    initial_value: 'false'
  - id: cycle_step
    type: int
    restore_value: no
    initial_value: '0'
  - id: last_touch_action_ms
    type: uint32_t
    restore_value: no
    initial_value: '0'

# -------------------------------------------------------------
# CONTINUOUS 2-SECOND HEARTBEAT SYNC ENGINE
# -------------------------------------------------------------
interval:
  - interval: 2s
    then:
      - lambda: |-
          bool states[18] = {
            id(sim_di_01), id(sim_di_02), id(sim_di_03), id(sim_di_04), id(sim_di_05), id(sim_di_06),
            id(sim_di_07), id(sim_di_08), id(sim_di_09), id(sim_di_10), id(sim_di_11), id(sim_di_12),
            id(sim_di_13), id(sim_di_14), id(sim_di_15), id(sim_di_16), id(sim_di_17), id(sim_di_18)
          };
          bool bypasses[18] = {
            id(sim_byp_01), id(sim_byp_02), id(sim_byp_03), id(sim_byp_04), id(sim_byp_05), id(sim_byp_06),
            id(sim_byp_07), id(sim_byp_08), id(sim_byp_09), id(sim_byp_10), id(sim_byp_11), id(sim_byp_12),
            id(sim_byp_13), id(sim_byp_14), id(sim_byp_15), id(sim_byp_16), id(sim_byp_17), id(sim_byp_18)
          };
          char buf[24];
          for (int i = 0; i < 18; i++) {
            // Send State
            sprintf(buf, "DI_%02d:%s", i + 1, states[i] ? "ON" : "OFF");
            send_esp_now_packet(buf);
            delay(6);
            // Send Bypass Status
            sprintf(buf, "DI_%02d:%s", i + 1, bypasses[i] ? "BYPASS_ON" : "BYPASS_OFF");
            send_esp_now_packet(buf);
            delay(6);
          }

  # Auto Sequential Cycle Tester (1.5s step)
  - interval: 1500ms
    then:
      - lambda: |-
          if (id(cycle_active)) {
            int step = id(cycle_step);
            int prev = (step + 17) % 18;

            char prev_buf[16];
            sprintf(prev_buf, "DI_%02d:OFF", prev + 1);
            send_esp_now_packet(prev_buf);
            delay(10);

            char cur_buf[16];
            sprintf(cur_buf, "DI_%02d:ON", step + 1);
            send_esp_now_packet(cur_buf);

            if (prev == 0) id(sim_di_01) = false;
            else if (prev == 1) id(sim_di_02) = false;
            else if (prev == 2) id(sim_di_03) = false;
            else if (prev == 3) id(sim_di_04) = false;
            else if (prev == 4) id(sim_di_05) = false;
            else if (prev == 5) id(sim_di_06) = false;
            else if (prev == 6) id(sim_di_07) = false;
            else if (prev == 7) id(sim_di_08) = false;
            else if (prev == 8) id(sim_di_09) = false;
            else if (prev == 9) id(sim_di_10) = false;
            else if (prev == 10) id(sim_di_11) = false;
            else if (prev == 11) id(sim_di_12) = false;
            else if (prev == 12) id(sim_di_13) = false;
            else if (prev == 13) id(sim_di_14) = false;
            else if (prev == 14) id(sim_di_15) = false;
            else if (prev == 15) id(sim_di_16) = false;
            else if (prev == 16) id(sim_di_17) = false;
            else if (prev == 17) id(sim_di_18) = false;

            if (step == 0) id(sim_di_01) = true;
            else if (step == 1) id(sim_di_02) = true;
            else if (step == 2) id(sim_di_03) = true;
            else if (step == 3) id(sim_di_04) = true;
            else if (step == 4) id(sim_di_05) = true;
            else if (step == 5) id(sim_di_06) = true;
            else if (step == 6) id(sim_di_07) = true;
            else if (step == 7) id(sim_di_08) = true;
            else if (step == 8) id(sim_di_09) = true;
            else if (step == 9) id(sim_di_10) = true;
            else if (step == 10) id(sim_di_11) = true;
            else if (step == 11) id(sim_di_12) = true;
            else if (step == 12) id(sim_di_13) = true;
            else if (step == 13) id(sim_di_14) = true;
            else if (step == 14) id(sim_di_15) = true;
            else if (step == 15) id(sim_di_16) = true;
            else if (step == 16) id(sim_di_17) = true;
            else if (step == 17) id(sim_di_18) = true;

            id(cycle_step) = (step + 1) % 18;
            id(my_display).update();
          }

# -------------------------------------------------------------
# HIGH-QUALITY TYPOGRAPHY & COLOR PALETTE
# -------------------------------------------------------------
font:
  - file: 'gfonts://Roboto'
    id: font_tiny
    size: 10
  - file: 'gfonts://Roboto'
    id: font_small
    size: 12
  - file: 'gfonts://Roboto'
    id: font_medium
    size: 14
  - file: 'gfonts://Roboto'
    id: font_header
    size: 16
  - file: 'gfonts://Roboto'
    id: font_large
    size: 20

color:
  - id: c_bg
    red: 0.08
    green: 0.09
    blue: 0.12
  - id: c_header
    red: 0.07
    green: 0.13
    blue: 0.22
  - id: c_border
    red: 0.22
    green: 0.25
    blue: 0.32
  - id: c_card_bg
    red: 0.12
    green: 0.14
    blue: 0.18
  - id: c_green
    red: 0.0
    green: 0.88
    blue: 0.32
  - id: c_red
    red: 0.95
    green: 0.22
    blue: 0.22
  - id: c_amber
    red: 1.0
    green: 0.60
    blue: 0.0
  - id: c_cyan
    red: 0.0
    green: 0.82
    blue: 1.0
  - id: c_white
    red: 1.0
    green: 1.0
    blue: 1.0
  - id: c_gray
    red: 0.70
    green: 0.72
    blue: 0.75
  - id: c_muted
    red: 0.45
    green: 0.48
    blue: 0.52

# -------------------------------------------------------------
# ST7796 3.5" TFT DISPLAY ENGINE (480 x 320 LANDSCAPE)
# -------------------------------------------------------------
display:
  - platform: mipi_spi
    model: st7796
    spi_id: tft_spi
    cs_pin: GPIO15
    dc_pin: GPIO2
    rotation: 90
    color_depth: 8bit
    color_order: bgr
    id: my_display
    update_interval: 500ms
    lambda: |-
      Color bg_color = id(c_bg);
      Color header_color = id(c_header);
      Color card_color = id(c_card_bg);
      Color border_color = id(c_border);
      Color text_white = id(c_white);
      Color text_cyan = id(c_cyan);
      Color text_gray = id(c_gray);
      Color text_muted = id(c_muted);
      Color status_green = id(c_green);
      Color status_red = id(c_red);
      Color status_amber = id(c_amber);

      // 1. Clear Screen
      it.fill(bg_color);

      // -------------------------------------------------------------
      // 2. Header Bar (38px High)
      // -------------------------------------------------------------
      it.filled_rectangle(0, 0, 480, 38, header_color);
      it.line(0, 38, 480, 38, id(bypass_mode_active) ? status_amber : text_cyan);

      // Mode Badge: [MODE: ALARM] or [MODE: BYPASS]
      if (id(bypass_mode_active)) {
        it.filled_rectangle(4, 4, 114, 30, status_amber);
        it.print(61, 11, id(font_small), bg_color, TextAlign::TOP_CENTER, "MODE: BYPASS");
      } else {
        it.filled_rectangle(4, 4, 114, 30, card_color);
        it.rectangle(4, 4, 114, 30, text_cyan);
        it.print(61, 11, id(font_small), text_cyan, TextAlign::TOP_CENTER, "MODE: ALARM");
      }

      // [ALL ON] Header Button
      it.filled_rectangle(124, 4, 62, 30, status_green);
      it.print(155, 11, id(font_small), bg_color, TextAlign::TOP_CENTER, "ALL ON");

      // [ALL OFF] Header Button
      it.filled_rectangle(190, 4, 64, 30, status_red);
      it.print(222, 11, id(font_small), text_white, TextAlign::TOP_CENTER, "ALL OFF");

      // [CLR BYP] Header Button
      it.filled_rectangle(258, 4, 68, 30, card_color);
      it.rectangle(258, 4, 68, 30, status_amber);
      it.print(292, 11, id(font_small), status_amber, TextAlign::TOP_CENTER, "CLR BYP");

      // [SET BYP] Header Button
      it.filled_rectangle(330, 4, 68, 30, card_color);
      it.rectangle(330, 4, 68, 30, status_amber);
      it.print(364, 11, id(font_small), status_amber, TextAlign::TOP_CENTER, "SET BYP");

      // [CYCLE] Header Button
      it.filled_rectangle(402, 4, 74, 30, id(cycle_active) ? status_amber : card_color);
      it.rectangle(402, 4, 74, 30, id(cycle_active) ? status_amber : text_cyan);
      it.print(439, 11, id(font_small), id(cycle_active) ? bg_color : text_cyan, TextAlign::TOP_CENTER, id(cycle_active) ? "CYCLING" : "CYCLE");

      // -------------------------------------------------------------
      // 3. 18-DI Interactive Touch Grid (6 Columns x 3 Rows)
      // -------------------------------------------------------------
      int start_x = 8;
      int start_y = 44;
      int btn_w = 72;
      int btn_h = 84;
      int gap_x = 7;
      int gap_y = 7;

      bool di_vals[18] = {
        id(sim_di_01), id(sim_di_02), id(sim_di_03), id(sim_di_04), id(sim_di_05), id(sim_di_06),
        id(sim_di_07), id(sim_di_08), id(sim_di_09), id(sim_di_10), id(sim_di_11), id(sim_di_12),
        id(sim_di_13), id(sim_di_14), id(sim_di_15), id(sim_di_16), id(sim_di_17), id(sim_di_18)
      };

      bool byp_vals[18] = {
        id(sim_byp_01), id(sim_byp_02), id(sim_byp_03), id(sim_byp_04), id(sim_byp_05), id(sim_byp_06),
        id(sim_byp_07), id(sim_byp_08), id(sim_byp_09), id(sim_byp_10), id(sim_byp_11), id(sim_byp_12),
        id(sim_byp_13), id(sim_byp_14), id(sim_byp_15), id(sim_byp_16), id(sim_byp_17), id(sim_byp_18)
      };

      for (int i = 0; i < 18; i++) {
        int col = i % 6;
        int row = i / 6;
        int bx = start_x + col * (btn_w + gap_x);
        int by = start_y + row * (btn_h + gap_y);

        bool is_on = di_vals[i];
        bool is_byp = byp_vals[i];

        // Button Container
        it.filled_rectangle(bx, by, btn_w, btn_h, is_byp ? (is_on ? status_amber : card_color) : (is_on ? status_green : card_color));
        it.rectangle(bx, by, btn_w, btn_h, is_byp ? status_amber : (is_on ? text_white : border_color));

        // DI Label + Bypass Flag
        char lbl[16];
        sprintf(lbl, "DI %02d", i + 1);
        it.print(bx + 6, by + 10, id(font_medium), is_byp ? status_amber : (is_on ? bg_color : text_white), lbl);
        if (is_byp) {
          it.print(bx + btn_w - 6, by + 12, id(font_tiny), status_amber, TextAlign::TOP_RIGHT, "BYP");
        }

        // State Text Box
        if (is_byp) {
          if (is_on) {
            it.print(bx + btn_w / 2, by + 46, id(font_large), bg_color, TextAlign::TOP_CENTER, "ON");
          } else {
            it.rectangle(bx + 6, by + 40, btn_w - 12, 34, status_amber);
            it.print(bx + btn_w / 2, by + 46, id(font_medium), status_amber, TextAlign::TOP_CENTER, "BYPASS");
          }
        } else if (is_on) {
          it.print(bx + btn_w / 2, by + 46, id(font_large), bg_color, TextAlign::TOP_CENTER, "ON");
        } else {
          it.filled_rectangle(bx + 8, by + 40, btn_w - 16, 34, status_red);
          it.print(bx + btn_w / 2, by + 46, id(font_large), text_white, TextAlign::TOP_CENTER, "OFF");
        }
      }

# -------------------------------------------------------------
# XPT2046 RESISTIVE TOUCHSCREEN CONTROLLER
# -------------------------------------------------------------
touchscreen:
  - platform: xpt2046
    id: hmi_touch
    spi_id: tft_spi
    cs_pin: GPIO33
    interrupt_pin: GPIO36
    calibration:
      x_min: 280
      x_max: 3860
      y_min: 340
      y_max: 3860
    transform:
      swap_xy: true
      mirror_x: true
      mirror_y: false
    on_touch:
      - lambda: |-
          uint32_t now = millis();
          if (now - id(last_touch_action_ms) < 250) {
            return; // 250ms Debounce
          }

          int x = touch.x;
          int y = touch.y;

          // 1. Header Action Buttons
          if (y >= 2 && y <= 40) {
            // [MODE TOGGLE: ALARM <-> BYPASS]
            if (x >= 4 && x <= 120) {
              id(last_touch_action_ms) = now;
              id(bypass_mode_active) = !id(bypass_mode_active);
              id(my_display).update();
              return;
            }
            // [ALL ON]
            if (x >= 122 && x <= 186) {
              id(last_touch_action_ms) = now;
              id(cycle_active) = false;
              id(sim_di_01) = true; id(sim_di_02) = true; id(sim_di_03) = true;
              id(sim_di_04) = true; id(sim_di_05) = true; id(sim_di_06) = true;
              id(sim_di_07) = true; id(sim_di_08) = true; id(sim_di_09) = true;
              id(sim_di_10) = true; id(sim_di_11) = true; id(sim_di_12) = true;
              id(sim_di_13) = true; id(sim_di_14) = true; id(sim_di_15) = true;
              id(sim_di_16) = true; id(sim_di_17) = true; id(sim_di_18) = true;
              id(my_display).update();
              send_esp_now_all(true);
              return;
            }
            // [ALL OFF]
            if (x >= 188 && x <= 254) {
              id(last_touch_action_ms) = now;
              id(cycle_active) = false;
              id(sim_di_01) = false; id(sim_di_02) = false; id(sim_di_03) = false;
              id(sim_di_04) = false; id(sim_di_05) = false; id(sim_di_06) = false;
              id(sim_di_07) = false; id(sim_di_08) = false; id(sim_di_09) = false;
              id(sim_di_10) = false; id(sim_di_11) = false; id(sim_di_12) = false;
              id(sim_di_13) = false; id(sim_di_14) = false; id(sim_di_15) = false;
              id(sim_di_16) = false; id(sim_di_17) = false; id(sim_di_18) = false;
              id(my_display).update();
              send_esp_now_all(false);
              return;
            }
            // [CLR BYP] Clear all bypasses
            if (x >= 256 && x <= 326) {
              id(last_touch_action_ms) = now;
              id(sim_byp_01) = false; send_esp_now_bypass(1, false);
              id(sim_byp_02) = false; send_esp_now_bypass(2, false);
              id(sim_byp_03) = false; send_esp_now_bypass(3, false);
              id(sim_byp_04) = false; send_esp_now_bypass(4, false);
              id(sim_byp_05) = false; send_esp_now_bypass(5, false);
              id(sim_byp_06) = false; send_esp_now_bypass(6, false);
              id(sim_byp_07) = false; send_esp_now_bypass(7, false);
              id(sim_byp_08) = false; send_esp_now_bypass(8, false);
              id(sim_byp_09) = false; send_esp_now_bypass(9, false);
              id(sim_byp_10) = false; send_esp_now_bypass(10, false);
              id(sim_byp_11) = false; send_esp_now_bypass(11, false);
              id(sim_byp_12) = false; send_esp_now_bypass(12, false);
              id(sim_byp_13) = false; send_esp_now_bypass(13, false);
              id(sim_byp_14) = false; send_esp_now_bypass(14, false);
              id(sim_byp_15) = false; send_esp_now_bypass(15, false);
              id(sim_byp_16) = false; send_esp_now_bypass(16, false);
              id(sim_byp_17) = false; send_esp_now_bypass(17, false);
              id(sim_byp_18) = false; send_esp_now_bypass(18, false);
              id(my_display).update();
              return;
            }
            // [SET BYP] Set all bypasses
            if (x >= 328 && x <= 398) {
              id(last_touch_action_ms) = now;
              id(sim_byp_01) = true; send_esp_now_bypass(1, true);
              id(sim_byp_02) = true; send_esp_now_bypass(2, true);
              id(sim_byp_03) = true; send_esp_now_bypass(3, true);
              id(sim_byp_04) = true; send_esp_now_bypass(4, true);
              id(sim_byp_05) = true; send_esp_now_bypass(5, true);
              id(sim_byp_06) = true; send_esp_now_bypass(6, true);
              id(sim_byp_07) = true; send_esp_now_bypass(7, true);
              id(sim_byp_08) = true; send_esp_now_bypass(8, true);
              id(sim_byp_09) = true; send_esp_now_bypass(9, true);
              id(sim_byp_10) = true; send_esp_now_bypass(10, true);
              id(sim_byp_11) = true; send_esp_now_bypass(11, true);
              id(sim_byp_12) = true; send_esp_now_bypass(12, true);
              id(sim_byp_13) = true; send_esp_now_bypass(13, true);
              id(sim_byp_14) = true; send_esp_now_bypass(14, true);
              id(sim_byp_15) = true; send_esp_now_bypass(15, true);
              id(sim_byp_16) = true; send_esp_now_bypass(16, true);
              id(sim_byp_17) = true; send_esp_now_bypass(17, true);
              id(sim_byp_18) = true; send_esp_now_bypass(18, true);
              id(my_display).update();
              return;
            }
            // [CYCLE]
            if (x >= 400 && x <= 478) {
              id(last_touch_action_ms) = now;
              id(cycle_active) = !id(cycle_active);
              id(cycle_step) = 0;
              id(my_display).update();
              return;
            }
          }

          // 2. 18-DI Buttons Grid
          int start_x = 8;
          int start_y = 44;
          int btn_w = 72;
          int btn_h = 84;
          int gap_x = 7;
          int gap_y = 7;

          for (int i = 0; i < 18; i++) {
            int col = i % 6;
            int row = i / 6;
            int bx = start_x + col * (btn_w + gap_x);
            int by = start_y + row * (btn_h + gap_y);

            if (x >= bx && x <= bx + btn_w && y >= by && y <= by + btn_h) {
              id(last_touch_action_ms) = now;
              id(cycle_active) = false;

              if (id(bypass_mode_active)) {
                // TOGGLE BYPASS FOR ZONE i
                if (i == 0) { id(sim_byp_01) = !id(sim_byp_01); send_esp_now_bypass(1, id(sim_byp_01)); }
                else if (i == 1) { id(sim_byp_02) = !id(sim_byp_02); send_esp_now_bypass(2, id(sim_byp_02)); }
                else if (i == 2) { id(sim_byp_03) = !id(sim_byp_03); send_esp_now_bypass(3, id(sim_byp_03)); }
                else if (i == 3) { id(sim_byp_04) = !id(sim_byp_04); send_esp_now_bypass(4, id(sim_byp_04)); }
                else if (i == 4) { id(sim_byp_05) = !id(sim_byp_05); send_esp_now_bypass(5, id(sim_byp_05)); }
                else if (i == 5) { id(sim_byp_06) = !id(sim_byp_06); send_esp_now_bypass(6, id(sim_byp_06)); }
                else if (i == 6) { id(sim_byp_07) = !id(sim_byp_07); send_esp_now_bypass(7, id(sim_byp_07)); }
                else if (i == 7) { id(sim_byp_08) = !id(sim_byp_08); send_esp_now_bypass(8, id(sim_byp_08)); }
                else if (i == 8) { id(sim_byp_09) = !id(sim_byp_09); send_esp_now_bypass(9, id(sim_byp_09)); }
                else if (i == 9) { id(sim_byp_10) = !id(sim_byp_10); send_esp_now_bypass(10, id(sim_byp_10)); }
                else if (i == 10) { id(sim_byp_11) = !id(sim_byp_11); send_esp_now_bypass(11, id(sim_byp_11)); }
                else if (i == 11) { id(sim_byp_12) = !id(sim_byp_12); send_esp_now_bypass(12, id(sim_byp_12)); }
                else if (i == 12) { id(sim_byp_13) = !id(sim_byp_13); send_esp_now_bypass(13, id(sim_byp_13)); }
                else if (i == 13) { id(sim_byp_14) = !id(sim_byp_14); send_esp_now_bypass(14, id(sim_byp_14)); }
                else if (i == 14) { id(sim_byp_15) = !id(sim_byp_15); send_esp_now_bypass(15, id(sim_byp_15)); }
                else if (i == 15) { id(sim_byp_16) = !id(sim_byp_16); send_esp_now_bypass(16, id(sim_byp_16)); }
                else if (i == 16) { id(sim_byp_17) = !id(sim_byp_17); send_esp_now_bypass(17, id(sim_byp_17)); }
                else if (i == 17) { id(sim_byp_18) = !id(sim_byp_18); send_esp_now_bypass(18, id(sim_byp_18)); }
              } else {
                // TOGGLE STATE FOR ZONE i
                if (i == 0) { id(sim_di_01) = !id(sim_di_01); send_esp_now_packet(id(sim_di_01) ? "DI_01:ON" : "DI_01:OFF"); }
                else if (i == 1) { id(sim_di_02) = !id(sim_di_02); send_esp_now_packet(id(sim_di_02) ? "DI_02:ON" : "DI_02:OFF"); }
                else if (i == 2) { id(sim_di_03) = !id(sim_di_03); send_esp_now_packet(id(sim_di_03) ? "DI_03:ON" : "DI_03:OFF"); }
                else if (i == 3) { id(sim_di_04) = !id(sim_di_04); send_esp_now_packet(id(sim_di_04) ? "DI_04:ON" : "DI_04:OFF"); }
                else if (i == 4) { id(sim_di_05) = !id(sim_di_05); send_esp_now_packet(id(sim_di_05) ? "DI_05:ON" : "DI_05:OFF"); }
                else if (i == 5) { id(sim_di_06) = !id(sim_di_06); send_esp_now_packet(id(sim_di_06) ? "DI_06:ON" : "DI_06:OFF"); }
                else if (i == 6) { id(sim_di_07) = !id(sim_di_07); send_esp_now_packet(id(sim_di_07) ? "DI_07:ON" : "DI_07:OFF"); }
                else if (i == 7) { id(sim_di_08) = !id(sim_di_08); send_esp_now_packet(id(sim_di_08) ? "DI_08:ON" : "DI_08:OFF"); }
                else if (i == 8) { id(sim_di_09) = !id(sim_di_09); send_esp_now_packet(id(sim_di_09) ? "DI_09:ON" : "DI_09:OFF"); }
                else if (i == 9) { id(sim_di_10) = !id(sim_di_10); send_esp_now_packet(id(sim_di_10) ? "DI_10:ON" : "DI_10:OFF"); }
                else if (i == 10) { id(sim_di_11) = !id(sim_di_11); send_esp_now_packet(id(sim_di_11) ? "DI_11:ON" : "DI_11:OFF"); }
                else if (i == 11) { id(sim_di_12) = !id(sim_di_12); send_esp_now_packet(id(sim_di_12) ? "DI_12:ON" : "DI_12:OFF"); }
                else if (i == 12) { id(sim_di_13) = !id(sim_di_13); send_esp_now_packet(id(sim_di_13) ? "DI_13:ON" : "DI_13:OFF"); }
                else if (i == 13) { id(sim_di_14) = !id(sim_di_14); send_esp_now_packet(id(sim_di_14) ? "DI_14:ON" : "DI_14:OFF"); }
                else if (i == 14) { id(sim_di_15) = !id(sim_di_15); send_esp_now_packet(id(sim_di_15) ? "DI_15:ON" : "DI_15:OFF"); }
                else if (i == 15) { id(sim_di_16) = !id(sim_di_16); send_esp_now_packet(id(sim_di_16) ? "DI_16:ON" : "DI_16:OFF"); }
                else if (i == 16) { id(sim_di_17) = !id(sim_di_17); send_esp_now_packet(id(sim_di_17) ? "DI_17:ON" : "DI_17:OFF"); }
                else if (i == 17) { id(sim_di_18) = !id(sim_di_18); send_esp_now_packet(id(sim_di_18) ? "DI_18:ON" : "DI_18:OFF"); }
              }
              id(my_display).update();
              break;
            }
          }
